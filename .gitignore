/claude-setup
/forgecrate
coverage.out

# Generierter Ordner — wird durch `forgecrate update` aus base/skills/ befüllt.
# Quelle der Wahrheit: base/skills/ (und flavors/*/skills/).
# Projekt-lokale Skills (nicht generiert) sind in .claude/skills/.gitignore explizit ausgenommen.

# Temporäre Agent-Worktrees (von Claude Code Sub-Agents erstellt)
.claude/worktrees/
# forgecrate:codegraph BEGIN
.codegraph/
# forgecrate:codegraph END
